{% set name = "sphinxcontrib-plantuml" %}
{% set version = "0.26" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/sphinxcontrib-plantuml-{{ version }}.tar.gz
  sha256: adb3397d5cb0613632cd3dad7894381422bac24464c393cb050404dd6712b1a7

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - pip
  run:
    - python >=3.8
    - sphinx >=1.6

test:
  imports:
    - sphinxcontrib
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/sphinx-contrib/plantuml/
  summary: 'Sphinx "plantuml" extension'
  license: BSD-2-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - melund
